<!--
The title of this pull request has to be a conventional commit subject
(feat, fix, docs, chore, test, ci, style, refactor, perf, revert). Merges are
squashed, so it becomes the commit subject release-please parses. A title
outside that form is dropped from the changelog and from the version bump.
-->

What changed, and why:

- [ ] Needs an ADR (`docs/adr/`) or a doc update, and it is in this branch. Leave unchecked if neither applies.
