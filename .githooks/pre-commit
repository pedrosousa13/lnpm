#!/usr/bin/env bash
set -euo pipefail

echo "[pre-commit] Running lint on staged changes..."

# Use the Makefile target for consistency
if ! make lint-staged; then
  echo "[pre-commit] Lint failed on staged changes." >&2
  exit 1
fi

echo "[pre-commit] Lint passed."